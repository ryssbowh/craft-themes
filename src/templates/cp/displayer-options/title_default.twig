{% import "_includes/forms" as forms %}

{{
    forms.selectField({
        options: options.getTags,
        value: options.tag,
        name: 'tag',
        label: 'Tag'|t('themes')
    })
}}

{{
    forms.lightswitchField({
        on: options.linked,
        name: 'linked',
        label: 'Link to Element'|t('themes')
    })
}}