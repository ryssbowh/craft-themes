{% import "_includes/forms" as forms %}

{% for site in sites %}
	{{ forms.selectField({
	    label: site.name,
	    name: 'themes['~site.uid~']',
	    options: themes,
	    value: settings.getHandle(site.uid)
	}) }}
{% endfor %}
