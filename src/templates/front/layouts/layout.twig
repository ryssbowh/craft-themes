<div class="{{ classes|raw }}"{% if attributes.size() %} {{ attributes|raw }}{% endif %}>
    {% for display in viewMode.visibleDisplays %}
        {{ display.render(element)|raw }}
    {% endfor %}
</div>