{% extends "blocks/block" %}

{% block content %}
    {% if currentUser %}
        {% set viewMode = craft.themes.viewModes.getByUid(block.options.viewMode) %}
        {{ craft.themes.view.renderLayout(block.userLayout, viewMode.handle, currentUser)|raw }}
    {% endif %}
{% endblock %}