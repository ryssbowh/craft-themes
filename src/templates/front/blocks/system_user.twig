{% if currentUser %}
    {% extends "blocks/block" %}
    
    {% block content %}
        {% set viewMode = craft.themes.viewModes.getByUid(block.options.viewMode) %}
        {{ craft.themes.view.renderLayout(block.userLayout, viewMode.handle, block.user)|raw }}
    {% endblock %}
{% endif %}