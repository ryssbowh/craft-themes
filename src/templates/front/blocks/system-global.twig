{% extends "blocks/block" %}

{% block content %}
    {% set viewMode = block.viewMode %}
    {% set globalSet = block.globalSet %}
    {% set layout = block.globalSetLayout %}
    {% if viewMode and layout and globalSet %}
        {{ craft.themes.view.renderLayout(layout, viewMode.handle, globalSet)|raw }}
    {% endif %}
{% endblock %}