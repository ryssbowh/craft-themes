{% extends "blocks/block" %}

{% block content %}
    {% set viewMode = block.viewMode %}
    {% set globalSet = block.globalSet %}
    {% if viewMode and globalSet %}
        {{ globalSet.layout.render(globalSet, viewMode)|raw }}
    {% endif %}
{% endblock %}