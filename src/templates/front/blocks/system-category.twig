{% extends "blocks/block" %}

{% block content %}
    {% for item in block.categories %}
        {% if item.viewMode and item.category %}
            {% set layout = item.category.group.layout %}
            {{ item.category.group.layout.render(item.category, item.viewMode) }}
        {% endif %}
    {% endfor %}
{% endblock %}