{% extends "blocks/block" %}

{% block content %}
    {% for display in layout.getViewMode(viewMode).getVisibleDisplays('default') %}
        {{ display.render(element)|raw }}
    {% endfor %}
{% endblock %}