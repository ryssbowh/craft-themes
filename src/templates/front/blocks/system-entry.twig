{% extends "blocks/block" %}

{% block content %}
    {% for item in block.entries %}
        {% if item.viewMode and item.entry %}
            {% set layout = item.entry.type.currentThemeLayout %}
            {{ craft.themes.view.renderLayout(layout, item.viewMode.handle, item.entry)|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}