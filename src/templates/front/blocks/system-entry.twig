{% extends "blocks/block" %}

{% block content %}
    {% for item in block.entries %}
        {% if item.viewMode and item.entry %}
            {% set layout = item.entry.type.layout %}
            {{ craft.themes.view.renderLayout(layout, item.viewMode, item.entry) }}
        {% endif %}
    {% endfor %}
{% endblock %}