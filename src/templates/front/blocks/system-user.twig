{% extends "blocks/block" %}

{% block content %}
    {% for item in block.users %}
        {% if item.viewMode and item.user %}
            {% set layout = item.user.layout %}
            {{ craft.themes.view.renderLayout(layout, item.viewMode.handle, item.user)|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}