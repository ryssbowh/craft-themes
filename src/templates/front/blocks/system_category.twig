{% extends "blocks/block" %}

{% block content %}
    {% for item in block.categories %}
        {% if item.viewMode and item.category %}
            {% set layout = item.category.group.currentThemeLayout %}
            {{ craft.themes.view.renderLayout(layout, item.viewMode.handle, item.category)|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}