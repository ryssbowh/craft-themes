{% blockcache %}
    <div class="{{ classes|raw }}"{% if attributes.size() %} {{ attributes|raw }}{% endif %}>
        {% block content %}
            {% if craft.app.config.general.devMode %}
                I'm the block {{ block.name }}, override my template 'blocks/{{ block.machineName }}.twig'
            {% endif %}
        {% endblock %}
    </div>
{% endblockcache %}