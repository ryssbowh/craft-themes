{% extends "fields/_field" %}

{% block content %}
    {% for asset in value %}
        {% set viewMode = options.getViewMode(asset) %}
        {% if viewMode %}
            {{ asset.volume.layout.render(asset, viewMode) }}
        {% endif %}
    {% endfor %}
{% endblock %}