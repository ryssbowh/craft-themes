{% extends "fields/_field" %}

{% block content %}
    {% set viewMode = options.viewMode %}
    {% if viewMode %}
        {% for category in value %}
            {{ category.group.layout.render(category, viewMode)|raw }}
        {% endfor %}
    {% endif %}
{% endblock %}