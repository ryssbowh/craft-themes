{% extends "fields/_field" %}
{% import "_includes/macros" as macros %}

{% block content %}
    {% set viewMode = options.viewMode %}
    {% if viewMode %}
        {% for category in value %}
            {{ macros.renderLayout(category.group.layout, viewMode, category) }}
        {% endfor %}
    {% endif %}
{% endblock %}