{% extends "fields/_field" %}
{% import "_includes/macros" as macros %}

{% set show_field = options.viewMode is not null %}

{% block content %}
    {% for tag in value %}
        {{ macros.renderLayout(displayer.tagLayout, options.viewMode, tag) }}
    {% endfor %}
{% endblock %}