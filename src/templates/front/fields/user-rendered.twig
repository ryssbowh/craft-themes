{% extends "fields/_field" %}

{% block content %}
    {% set layout = displayer.userLayout %}
    {% set viewMode = options.viewMode %}
    {% if layout and viewMode %}
        {% for user in value %}
            {{ layout.render(user, viewMode) }}
        {% endfor %}
    {% endif %}
{% endblock %}