{% extends "fields/_field" %}

{% block content %}
    {% if options.viewMode %}
        {% for user in value %}
            {{ displayer.userLayout.render(user, options.viewMode.handle)|raw }}
        {% endfor %}
    {% endif %}
{% endblock %}