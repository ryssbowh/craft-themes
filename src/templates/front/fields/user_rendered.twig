{% extends "fields/_field" %}

{% block content %}
    {% if options.viewMode %}
        {% set viewMode = craft.themes.viewModes.getByUid(options.viewMode) %}
        {% for user in value %}
            {{ craft.themes.view.renderLayout(displayer.userLayout, viewMode.handle, user)|raw }}
        {% endfor %}
    {% endif %}
{% endblock %}