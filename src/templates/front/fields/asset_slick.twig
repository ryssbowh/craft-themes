{% extends "fields/_field" %}

{% block content %}
    <div class="slick-carousel" data-slick="{{ options.getAttributes(null, ['viewModes'])|json_encode }}">
        {% for asset in value %}
            {% set viewMode = options.getViewMode(asset) %}
            {% if viewMode %}
                {{ asset.volume.layout.render(asset, viewMode.handle)|raw }}
            {% endif %}
        {% endfor %}
    </div>
{% endblock %}