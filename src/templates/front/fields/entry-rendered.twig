{% extends "fields/_field" %}

{% block content %}
    {% for entry in value %}
        {% set viewMode = options.getViewMode(entry) %}
        {% if viewMode and layout %}
            {{ entry.type.layout.render(entry, viewMode) }}
        {% endif %}
    {% endfor %}
{% endblock %}