{% extends "fields/_field" %}
{% import "_includes/macros" as macros %}

{% block content %}
    {% for entry in value %}
        {{ macros.renderLayout(entry.type.layout, options.getViewMode(entry), entry) }}
    {% endfor %}
{% endblock %}