{% extends "fields/_field" %}

{% block content %}
    {% set viewMode = craft.themes.viewModes.getByUid(options.viewMode) %}
    {% for tag in value %}
        {{ craft.themes.view.renderLayout(displayer.tagLayout, viewMode.handle, tag)|raw }}
    {% endfor %}
{% endblock %}