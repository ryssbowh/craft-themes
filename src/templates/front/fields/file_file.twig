{% extends "fields/_field" %}

{% set show_field = value|length > 0 %}

{% block content %}
    {{ craft.themes.view.renderFile(value, field, options.getDisplayerForKind(value.kind))|raw }}
{% endblock %}