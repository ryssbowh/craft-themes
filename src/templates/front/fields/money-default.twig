{% extends "fields/_field" %}

{% block content %}
    {% if options.showCurrency %}
        {{ value.getCurrency }}
    {% endif %}
    {{ (value.amount/100)|number_format(2, '.', ',') }}
{% endblock %}