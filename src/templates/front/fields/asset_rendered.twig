{% extends "fields/_field" %}

{% block content %}
    {% for asset in value %}
        {% set viewMode = options.getViewMode(asset) %}
        {% if viewMode %}
            {{ asset.volume.currentThemeLayout.render(asset, viewMode.handle)|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}