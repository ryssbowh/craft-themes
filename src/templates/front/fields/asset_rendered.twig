{% extends "fields/_field" %}

{% block content %}
    {% for asset in value %}
        {% set viewMode = options.getViewMode(asset) %}
        {% if viewMode %}
            {{ craft.themes.view.renderLayout(asset.volume.currentThemeLayout, viewMode.handle, asset)|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}