{% extends "fields/_field" %}

{% block content %}
    <div class="slick-carousel" data-slick="{{ options.getAttributes(null, ['viewModeUid'])|json_encode }}">
        {% for category in value %}
            {{ displayer.groupLayout.render(category, options.viewMode.handle) }}
        {% endfor %}
    </div>
{% endblock %}