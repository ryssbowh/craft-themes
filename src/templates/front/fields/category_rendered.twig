{% extends "fields/_field" %}

{% block content %}
    {% if value.0 is defined %}
        {% set viewMode = craft.themes.viewModes.getByUid(options.viewMode) %}
        {{ craft.themes.view.renderLayout(displayer.groupLayout, viewMode.handle, value.0)|raw }}
    {% endif %}
{% endblock %}