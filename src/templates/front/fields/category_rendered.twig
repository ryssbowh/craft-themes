{% extends "fields/_field" %}

{% set show_field = value|length > 0 and option.viewMode %}

{% block content %}
    {% for category in value %}
        {{ displayer.groupLayout.render(category, options.viewMode.handle)|raw }}
    {% endfor %}
{% endblock %}