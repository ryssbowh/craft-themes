{% extends "fields/_field" %}

{% block content %}
    {% if options.viewMode %}
        {% set viewMode = craft.themes.viewModes.getByUid(options.viewMode) %}
        {% for category in value %}
            {{ craft.themes.view.renderLayout(displayer.groupLayout, viewMode.handle, category)|raw }}
        {% endfor %}
    {% endif %}
{% endblock %}