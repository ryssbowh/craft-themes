{% extends "fields/_field" %}

{% block content %}
    {% set viewMode = options.viewMode %}
    {% set layout = displayer.groupLayout %}
    {% if viewMode and layout %}
        {% for category in value %}
            {{ layout.render(category, viewMode)|raw }}
        {% endfor %}
    {% endif %}
{% endblock %}