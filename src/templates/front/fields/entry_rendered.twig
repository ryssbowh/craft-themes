{% extends "fields/_field" %}

{% block content %}
    {% for entry in value %}
        {% set layout = entry.type.layout %}
        {% set viewMode = options.getEntryViewMode(entry) %}
        {% if viewMode and layout %}
            {{ layout.render(entry, viewMode.handle)|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}