{% extends "fields/_field" %}

{% block content %}
    {% if options.viewMode %}
        {% set viewMode = craft.themes.viewModes.getByUid(options.viewMode) %}
        {% for entry in value %}
            {{ craft.themes.view.renderLayout(displayer.groupLayout, viewMode.handle, entry)|raw }}
        {% endfor %}
    {% endif %}
{% endblock %}