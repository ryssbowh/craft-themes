{% extends "files/_file" %}

{% block content %}
    {% set transform = options.transform == '_custom' ? options.custom|json_decode : options.transform %}
    {% set sizes = options.transform == '_custom' ? options.sizes|json_decode : null %}
    {{ asset.getImg(transform, sizes) }}
{% endblock %}